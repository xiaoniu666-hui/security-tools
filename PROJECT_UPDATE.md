# 网络安全工具集 - 项目更新文档

## 版本历史

| 版本 | 日期 | 主要变更 |
|------|------|----------|
| 1.0.0 | 2026-04-29 | 初始版本 |
| 2.0.0 | 2026-04-30 | 商用升级 |
| 2.1.0 | 2026-05-03 | 新增攻击测试功能 |
| 2.2.0 | 2026-05-04 | 全面测试与Bug修复 |
| 2.3.0 | 2026-05-12 | 全面测试与Bug修复 v2 |

---

## v1.0.0 → v2.0.0 升级变更 (2026-04-30)

### 后端升级

#### 1. 认证系统升级
| 变更 | 说明 |
|------|------|
| 新增JWT认证 | 从localStorage迁移到JWT Token机制 |
| 实现accessToken + refreshToken | accessToken 1小时，refreshToken 7天 |
| bcrypt密码加密 | 12轮哈希，安全存储密码 |
| 账号锁定机制 | 15分钟内3次失败自动锁定 |
| 用户会话管理 | 新增`user_sessions`表存储会话 |

#### 2. 数据库升级
| 变更 | 说明 |
|------|------|
| 引入MySQL | 从本地存储迁移到MySQL数据库 |
| 连接池优化 | 配置10个连接的连接池 |
| 时区修复 | 修复Token过期时间时区问题 |
| 数据库表 |
| - `users` | 用户信息表 |
| - `user_sessions` | 用户会话表 |
| - `audit_logs` | 审计日志表 |
| - `blacklisted_ips` | IP黑名单表 |
| - `waf_rules` | WAF规则表 |

#### 3. 安全增强
| 变更 | 说明 |
|------|------|
| Helmet中间件 | CSP、HSTS、X-Frame-Options安全头 |
| CORS白名单 | 配置允许的前端域名 |
| 速率限制 | 登录(3次/15分钟)和API请求限制 |
| 输入验证 | 参数校验和防注入检查 |
| 审计日志 | 记录关键操作和安全事件 |

#### 4. 新功能
| 功能 | 说明 |
|------|------|
| 漏洞扫描 | 支持15+种漏洞检测，含修复建议 |
| AI助手增强 | 扩展安全知识问答库 |
| 日志系统 | Winston日志，文件+控制台输出 |

### 前端升级

#### 1. 认证改进
| 变更 | 说明 |
|------|------|
| axios拦截器 | 自动添加Authorization Header |
| 认证检查加载状态 | 检查完成前显示loading动画 |
| Token刷新 | 自动刷新过期的accessToken |

#### 2. 组件优化
| 变更 | 说明 |
|------|------|
| 空值检查 | 修复`undefined.length`错误 |
| 错误提示增强 | 更友好的错误提示信息 |

---

## v2.0.0 → v2.1.0 升级变更 (2026-05-03)

### 新增攻击测试功能

#### 1. CSRF攻击测试
- **功能**: 检测目标URL的CSRF漏洞
- **接口**: `POST /api/attack/csrf-test`
- **参数**:
  - `url`: 目标URL
  - `method`: 请求方法 (GET/POST)
  - `csrf_token`: (可选)CSRF Token
- **检测项**:
  - Token缺失或过短
  - 使用GET方法执行敏感操作
  - 缺少Referer验证

#### 2. 命令注入检测
- **功能**: 检测输入中的命令注入风险
- **接口**: `POST /api/attack/command-injection`
- **参数**:
  - `input`: 测试输入
- **检测模式**:
  - Unix命令 (|、;、$(...)、`...`)
  - Windows命令 (&、|、%...%)
  - SQL注入 (UNION、DROP、--)

#### 3. 路径遍历检测
- **功能**: 检测路径遍历/目录遍历漏洞
- **接口**: `POST /api/attack/path-traversal`
- **参数**:
  - `path`: 测试路径
  - `base_dir`: 基础目录限制
- **检测模式**:
  - `../` 标准遍历
  - 编码遍历 (`%2e%2e%2f`)

#### 4. SSRF测试
- **功能**: 检测服务端请求伪造漏洞
- **接口**: `POST /api/attack/ssrf-test`
- **参数**:
  - `url`: 目标URL
- **检测项**:
  - 内网IP访问 (10.*, 172.16-31.*, 192.168.*)
  - 本地访问 (127.0.0.1, localhost)
  - 元数据服务 (169.254.169.254)
  - 本地文件协议 (file://)

### 前端新增组件
| 组件 | 文件 | 功能 |
|------|------|------|
| CSRFTest | `CSRFTest.vue` | CSRF攻击测试界面 |
| CommandInjection | `CommandInjection.vue` | 命令注入检测界面 |
| PathTraversal | `PathTraversal.vue` | 路径遍历检测界面 |
| SSRFTest | `SSRFTest.vue` | SSRF测试界面 |

### 后端新增路由
| 文件 | 路径 | 说明 |
|------|------|------|
| attack.js | `routes/attack.js` | 攻击测试API路由 |

### 数据库新增表
| 表名 | 说明 |
|------|------|
| `domain_records` | 域名查询记录表 |
| `vpn_proxies` | VPN代理记录表 |
| `ip_spoofing_attempts` | IP欺骗尝试记录 |
| `network_scans` | 网络扫描记录 |

---

## v2.1.0 → v2.2.0 升级变更 (2026-05-04)

### Bug修复

#### 1. 防御系统Bug
| 问题 | 修复 |
|------|------|
| 黑名单查询失败 | 添加`blacklisted_ips`表 |
| WAF规则查询失败 | 修复查询使用`priority`列改为`id` |
| 安全事件查询失败 | 修复列名`timestamp`→`created_at`、`level`→`severity` |
| MySQL LIMIT参数错误 | 改用固定LIMIT 50避免参数问题 |

#### 2. 数据库完整性
| 新增表 | 说明 |
|--------|------|
| `blacklisted_ips` | 完整的黑名单管理表 |
| `security_events` | 安全事件记录表 |

### 测试改进
| 新增 | 说明 |
|------|------|
| test_all_api.js | 全面API测试脚本 |
| 15个核心接口测试 | 覆盖所有主要功能模块 |

---

## 技术架构演进对比

### v1.0.0架构
```
本地存储 (JSON files)
├── 用户存储
├── 会话存储
└── 数据存储

前端: Vue 3 + localStorage
后端: Express (轻量级)
安全: 基础认证
```

### v2.2.0架构
```
MySQL数据库
├── users
├── user_sessions
├── audit_logs
├── blacklisted_ips
├── waf_rules
├── security_events
├── domain_records
├── vpn_proxies
├── ip_spoofing_attempts
└── network_scans

前端: Vue 3 + JWT + axios拦截器
后端: Express + Helmet + 速率限制
安全: JWT认证 + bcrypt + 审计日志
日志: Winston文件+控制台
```

---

## 功能对比

| 功能模块 | v1.0.0 | v2.0.0 | v2.2.0 |
|---------|--------|--------|--------|
| **攻击手段** |
| XSS测试 | ✅ | ✅ | ✅ |
| 密码破解 | ✅ | ✅ | ✅ |
| SQL注入 | ✅ | ✅ | ✅ |
| 信息爬虫 | ✅ | ✅ | ✅ |
| CSRF测试 | ❌ | ❌ | ✅ 新增 |
| 命令注入 | ❌ | ❌ | ✅ 新增 |
| 路径遍历 | ❌ | ❌ | ✅ 新增 |
| SSRF测试 | ❌ | ❌ | ✅ 新增 |
| **防御手段** |
| 黑名单管理 | ✅ | ✅ | ✅ |
| WAF规则 | ✅ | ✅ | ✅ |
| 安全事件 | ✅ | ✅ | ✅ 修复 |
| SSL证书 | ✅ | ✅ | ✅ |
| 入侵检测 | ✅ | ✅ | ✅ |
| **辅助工具** |
| IP查询 | ✅ | ✅ | ✅ |
| 域名查询 | ✅ | ✅ | ✅ |
| VPN分析 | ✅ | ✅ | ✅ 修复 |
| 漏洞扫描 | ❌ | ✅ | ✅ |
| **AI助手** |
| 安全问答 | ✅ | ✅ 增强 | ✅ |
| **安全特性** |
| JWT认证 | ❌ | ✅ | ✅ |
| bcrypt加密 | ❌ | ✅ | ✅ |
| 速率限制 | ❌ | ✅ | ✅ |
| Helmet安全头 | ❌ | ✅ | ✅ |
| 审计日志 | ❌ | ✅ | ✅ |
| 账号锁定 | ❌ | ✅ | ✅ |
| **测试** |
| API测试 | ❌ | ✅ 基础 | ✅ 全面 |
| 完整测试脚本 | ❌ | ❌ | ✅ |

---

## 已知问题与解决方案

### 历史问题记录

#### 问题1: 登录失败次数过多 (多次出现)
- **问题**: 测试时触发速率限制，账号锁定15分钟
- **原因**: test_all_api.js多次登录
- **解决方案**: 
  - 登录成功后调用resetLoginLimit
  - 测试间隔添加sleep
  - 重启服务器清除内存中的限制

#### 问题2: API端点不存在 (2026-04-30)
- **问题**: 部分接口调用404
- **原因**: 路由注册顺序问题
- **解决方案**: 修复路由名称、确保正确注册

#### 问题3: 路径遍历检测服务器错误 (2026-05-04)
- **问题**: 路径遍历测试失败
- **原因**: 服务器需要重启应用新表结构
- **解决方案**: 重启后端服务器

---

## 测试验证结果

### v2.2.0测试状态 (2026-05-04)
**通过**: 15/19个核心接口
**部分需要参数**: 4个接口需要完整参数才能正常工作

### 测试脚本
`d:\网安\网安\backend\test_all_api.js`

---

## v2.2.0 → v2.3.0 升级变更 (2026-05-12)

### Bug修复与改进

#### 1. 认证系统
- **新增** `/api/auth/check` 端点，用于验证当前认证状态
- **改进** 认证中间件兼容性，支持更灵活的 token 验证

#### 2. 域名查询
- **新增** POST 方法支持的 `/api/domain/lookup` 端点
- **改进** 统一查询接口，同时支持 GET 和 POST 请求

#### 3. VPN分析
- **新增** POST 方法支持的 `/api/vpn/analyze` 端点
- **改进** 简化 API 调用，提高集成效率

#### 4. 密码破解
- **新增** `/api/crack/test` 端点，用于密码强度检测
- **功能** 检测弱密码、评分密码强度、提供改进建议

#### 5. 路径遍历检测
- **修复** 变量名拼写错误 (`escapeBasedir` → `escape_basedir`)
- **改进** 错误处理，提供更清晰的响应

#### 6. 防御系统
- **新增** `/api/defense/waf-rules` 端点别名
- **新增** `/api/defense/security-events` 端点别名
- **改进** 统一 API 命名规范，提高开发体验

### 新增测试工具

#### 完整 API 测试套件
- **文件** `backend/test-all-api.js`
- **功能** 覆盖所有核心功能模块的自动化测试
- **统计** 20个测试用例，成功率 95%
- **报告** 详细的成功/失败统计信息

### 测试验证结果

#### v2.3.0 测试状态 (2026-05-12)
- **成功率** 95.0%
- **总测试数** 20
- **通过** 19个核心接口
- **失败** 1个(用户已存在，预期行为)

#### 测试覆盖范围
1. ✅ 健康检查
2. ✅ 版本检查
3. ✅ 用户注册/登录
4. ✅ 认证状态验证
5. ✅ IP查询
6. ✅ 域名查询
7. ✅ VPN分析
8. ✅ SQL注入测试
9. ✅ XSS测试
10. ✅ 密码破解测试
11. ✅ 漏洞扫描信息
12. ✅ CSRF攻击测试
13. ✅ 命令注入检测
14. ✅ 路径遍历检测
15. ✅ SSRF测试
16. ✅ 黑名单管理
17. ✅ WAF规则
18. ✅ 安全事件
19. ✅ AI助手

---

## 未来计划

### 下一版本 (v2.4.0) 计划
- [ ] 完整的爬虫功能端点
- [ ] XSS测试增强
- [ ] 密码破解测试增强
- [ ] SQL注入测试增强
- [ ] 更详细的漏洞扫描报告
- [ ] 告警通知系统
